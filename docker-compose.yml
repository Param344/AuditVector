version: '3.8'

services:
  auditvector:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - GEMINI_MODEL=gemini-3.5-flash
      - AUDITVECTOR_OFFLINE_MODE=false
      # - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    restart: unless-stopped
