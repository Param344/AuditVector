# 🎥 AUDITVECTOR — ~4-MINUTE DEMO VIDEO RECORDING SCRIPT

Use this structured script to record your submission video for the All Things Agentic Hackathon.

---

### ⏱️ Video Structure Breakdown

| Timestamp | Section | Visual On-Screen | Voiceover / Talking Points |
| :--- | :--- | :--- | :--- |
| **0:00 - 0:40** | **1. The Problem & Core Thesis** | Title slide + Architecture Diagram in `README.md` | *"Millions of dollars in quantitative finance and algorithmic trading are lost not because strategies fail, but because the software calculating their performance contains silent integrity bugs—inverted signs, double-deducted commissions, and config drift. LLMs cannot solve this by guessing arithmetic on millions of rows. That's why we built **AuditVector**: an autonomous multi-agent financial integrity investigator. Our principle: **AI reasons. Code proves. Evidence explains.**"* |
| **0:40 - 1:15** | **2. Launchpad & Architecture** | Web UI at `localhost:8000` (Launchpad screen) | *"Here is the AuditVector Forensic Command Console. Under the hood, we are orchestrating 5 Google ADK agents powered by Gemini 3.5 Flash running asynchronously on Google Cloud Run, backed by Google Cloud Pub/Sub and Firestore. Let's run our first benchmark: **IntegrityLab Alpha**, a synthetic quantitative backtester with 4 planted silent integrity flaws."* |
| **1:15 - 2:05** | **3. Live Investigation & Alpha Contradictions** | Click `START AUDIT` on Alpha. Watch live 5-agent pipeline transition. Show Verdict screen. | *"Notice the live telemetry stream: Audit Planner scopes the repository; Repository Investigator parses AST logic without sending entire codebases to the LLM; Financial Investigator extracts reported claims; Contradiction Investigator invokes deterministic FIFO lot matchers and DuckDB SQL; and Report Agent seals the findings. In under a second, AuditVector reveals **$44,140.00 in capital at risk** across 4 verified contradictions. Look at the **Claim vs. Reality** hero panel: the system claimed a positive $18,240 PnL and +18.24% return, but deterministic FIFO reconstruction proves an actual net loss of -$3,720—a critical -$21,960 variance."* |
| **2:05 - 2:45** | **4. Evidence Contract & Interactive Provenance Graph** | Click `INSPECT EVIDENCE CONTRACT` on F-001. Switch to `Interactive Evidence Graph` tab. Click graph nodes. | *"AuditVector enforces a strict mandate: **No Evidence Contract $\to$ No Verified Finding**. When we inspect finding F-001, we see exact source citations (`strategy_alpha.py:1-50`), SHA-256 cryptographic hashes, canonical normalizer versions, and verifier algorithms. In the Interactive Evidence Graph, judges can click through each node from raw CSV to normalized financial events to deterministic proof."* |
| **2:45 - 3:20** | **5. Control Benchmark (Zero False Positives)** | Click `+ New Audit` $\to$ `IntegrityLab Control` | *"Now let's test whether our agents hallucinate. We run **IntegrityLab Control**, a clean baseline. In moments, the UI switches to emerald green: **FINANCIAL INTEGRITY VERIFIED**, with exactly **$0.00** discrepancy and 0 false contradictions. All claims match deterministic bottom-up reconstruction."* |
| **3:20 - 3:45** | **6. Real-World Dogfood (AI-BIP)** | Click `+ New Audit` $\to$ `AI-BIP Quantitative Engine` | *"Finally, we dogfooded AuditVector on **AI-BIP**, a real-world multi-asset momentum strategy across BTC, ETH, SOL, and AVAX. AuditVector immediately uncovered a **$16,286.24** discrepancy where the strategy's backtest report assumed a frictionless 8.0 bps fee model while actual execution fills experienced significant slippage."* |
| **3:45 - 4:00** | **7. Conclusion & Google Cloud Backend Proof** | Show terminal/Cloud Run deployment logs + `verify_all.sh` passing all 55 tests. | *"All 55 tests pass in 1.2 seconds, with full deployment scripts for Google Cloud Run, Pub/Sub, and Firestore. AuditVector brings deterministic proof and cryptographic grounding to autonomous financial agents. Thank you!"* |
